# AGENT-PLATFORM-UX-PLAN · 更好用、更易用优化计划

> 2027-09 制定（走查实证——不含臆想）——apps/agent-platform 可用性优化。
> 现状基线：交互面已相当完善（空态引导/断线提示/工具详情/流式状态/重试继续/
> 超时可见化/呼吸灯/消息导出/@ 定向/断线补偿——均为既有能力）。
> 本计划只做「走查发现的真实缺口」——每波单一主题——小步快跑。
> 完成状态：波次 1 ✅（拖拽上传——流式光标判负）· 波次 2-6 ⬜

---

## 走查结论（实证——不拍脑袋）

真实浏览器走查 12 页（注册→遍历，console 错误全零）+ 代码核查：

| 面 | 现状 | 缺口（证据） |
| --- | --- | --- |
| 空态引导 | 各页 EmptyState + CTA（工作台三步/Agent/部门/会话） | 注册首访无分步链（走到「新建空间」即断——后续步骤靠文案提示） |
| 文件上传 | 按钮（pickFile——input file） | **无拖拽上传**（现代 IM 标配——拖文件入消息区即传） |
| 流式视觉 | 状态标签（思考中/生成中）+ 工具步骤 | **无流式光标**（内容尾部跳动符——生成感知增强） |
| AI 状态 | 呼吸灯/工具 args+result/超时 60s 可见/重试/继续 | 完好 |
| WS 断线 | 顶部 Badge「连接断开」 | 完好（补拉/轮询已锁） |
| Agent 配置 | AgentDetail 615 行长表单 | **无分区**（基础/能力/知识/高级混排——配置可发现性低） |
| NewAgent | 357 行表单 | **无模板预填**（模板市场已有——创建时不经模板走） |
| 工程健康 | Chat.tsx 961 行（事件/渲染/工具/文件逻辑混编） | **模块化缺口**（纯重构收益——维护性） |
| 移动端 | 左栏窄屏隐藏 | 待验（游戏面——波次 6 验收顺手查） |

---

## 波次计划

### 波次 1：对话体验增强——拖拽上传 + 流式光标 ✅（2027-09——`6df4d16c`）
> **流式光标项审计判负**：框架 MessageBubble 已有 `wf-bubble--streaming`
> 右侧边框闪烁（1s 平缓 + reduced-motion 降级——合规）——▍ 光标属装饰
> 重复——判负记录（与波次 4/6 同款纪律）。
- 拖拽上传：消息区（bodyEl）dragover/drop 监听——文件落区即走上传链（onFilePick 复用）
  - 拖入高亮（视觉反馈——drag-over 样式）
  - 多文件（与按钮一致——单文件语义保持）
  - 禁止拖拽时误触（非文件拖入忽略）
- 流式光标：MessageItem 生成中（status generating 且内容非空）内容尾部「▍」跳动
  - CSS 动画（wf-breathe 同款——零 JS 定时器——渲染路径零副作用）
- 验收：e2e（拖拽文件注入——DataTransfer 构造——文件卡片出现）；comp/场景断言光标存在；
  契约回归全绿

### 波次 2：AgentDetail 配置分区（配置流畅性）⬜
- 长表单分区：基础信息 / 能力（工具+技能绑定）/ 知识库 / 高级（HITL/模型/配额）
  - 分区形态：折叠卡片（Accordion——默认全展开？）或分区标题分组——降低「一屏看晕」
  - 分区导航（顶部锚点/侧栏——快速跳转）
- 验收：页面走查（分区渲染/跳转）；既有 test/ui/agents.test.ts 回归
- **判负考察点**：若分区增加交互成本 > 收益（表单已短）——按审计定论收窄

### 波次 3：NewAgent 模板预填（创建成本）⬜
- 创建页「从模板开始」：模板市场（role-templates）选择 → 预填表单
  - 预填面：name/system_prompt/tools/skill 绑定/worker 参数（模板定义面）
  - 无模板仍走自定义（现有行为保留）
- 验收：模板选择 → 表单预填断言；role-templates.test.ts 回归

### 波次 4：注册首访 onboarding 链（首次体验）⬜
- 三步入门链：① 建项目空间 → ② 添加 AI Agent → ③ 试着 @AI 干活
  - 工作台首访卡片（已有三步文案）→ 每步完成后续接下一步（状态推进）
  - 完成后收卡（onboarding 完成标记——localStorage）
- 验收：首访步进断言（UI 测试——存储标记分步）；走查

### 波次 5：Chat.tsx 模块化拆分（工程健康——纯重构）⬜
- 拆分面：`chat-events.ts`（WS 事件处理/ensureAiMsg/超时扫描）、`chat-handlers.ts`
  （sendText/retry/continue/搜索/文件）、`chat-render.tsx`（消息列表/左栏/输入区——
  或保持页面内——拆分后页面 ≤400 行）
- **纪律**：零行为变更——仅移动——拆分前后全量测试对比（stash 前后类型错误对比——
  R-03 迁移纪律）
- 验收：全量 305 回归 + tsc；拆分后页面行数报告

### 波次 6：验收与归档 ⬜
- 全量回归（agent-platform 305 + 契约 359 + 场景 120）
- 走查复测（12 页零错误 + 关键交互）
- 计划归档（头部完成标记 + 波次提交锚点——仓库惯例）

---

## 判负登记（预登记——审计后再定）
- 消息列表虚拟化：200 条截断已在——长会话性能缺实证——**预判负**（需求驱动再启）
- 移动端聊天：窄屏已隐藏左栏——输入/发送可用——**波次 6 走查定论**

## 验收基线（全绿门禁）
agent-platform 305（291+14skip+0fail）· 框架契约 359 · 场景 120 · tsc 0 · audit:semantics 0
