/**
 * 社区组件收录表（01 生态计划 P1 /community 域）
 * 外部贡献台阶：组件（三件套 + demo + 质量 checklist）→ 提交 PR → 收录于此。
 * 提交模板见 /community 页面（GitHub issue template: component-request.md）。
 * 质量门槛（与内置组件同标）：三件套 + style-audit 合规 + 键盘/响应式/主题状态矩阵。
 */
import type { CommunityEntry } from './types.ts'

export const community: CommunityEntry[] = [
  // 首个外部贡献收录于此（PR 合入后登记——字段：name/desc/author/url/quality）
]
