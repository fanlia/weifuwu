# weifuwu — 开发者指南

> 面向 weifuwu **框架开发者/贡献者**：架构约束、编码标准、内部机制。
> **本文件随开发推进逐步补充**（2026-12 重建——删除自过期版本后重新记录）。

---

## 1. 测试命令纪律

| ID | 规则 | 说明 |
| --- | --- | --- |
| R-01 | **运行测试相关命令的 timeout 最多 10 秒** | 测试/脚本卡住（挂起/竞态/等待）时，用更短 timeout 复跑缩小范围定位，减少浪费等待的时间——超时即信号，不无限等待 |

## 2. 测试分层（新内置测试框架）

```
契约层（src/test/contract/）——命令流断言——node 直跑——零浏览器
场景层（src/test/scenario/）——DOM 行为断言——SSR 服务化 + playwright——真实浏览器
```

（其余章节随开发完成补充）
