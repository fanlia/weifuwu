# 全栈体验提升（2027-xx）

> 一句话目标：**跨端缝收口**——前端类型从后端 shape 单源派生（37 个手动
> interface 双写 → 0）+ 权限声明单源（前后端同源）+ 端点类型面评估（判负
> 候选）+ 全栈测试动线文档化。
> 动机（消费证据）：全栈体验分析的结论是「**体验瓶颈不在两端各自能力，而在
> 两端之间的缝**」——前端 `lib/types.ts` 37 interface 手动双写（Agent 40
> 字段——改后端字段前端不红，漂移靠测试抓）· 权限前后端各一份（21
> `require*` vs 8 `can*`——规则改两处）· 74 处 URL 字符串（无端点类型面）—
> 后端 `RowOf`/`BodyOf` 类型面已就位（orm 体验提升 W0-W4 交付）——跨端派生
> 的前置条件已满足。

## 现状探针（2027-xx 读数）

| 面 | 现状 | 目标 |
| --- | --- | --- |
| 前端手动 interface/type（双写后端 shape） | **37 个**（Agent 40 字段——含响应附加字段 token_usage 等） | → 0（RowOf 派生——响应附加字段用交叉扩展） |
| API URL 字符串（`ctx.api.get('/api/...')`） | **74 处** | → 端点类型面（评估——判负候选） |
| 权限双写 | 21 `require*`（后端）+ 8 `can*`（前端 roles） | → 单源声明（roles 定义共享） |
| 认证接线时序 | onRefresh 已修（闭包直引）——**教训登记** | → 接线时序契约（auth client 面文档化） |
| 前后端共享模块 | shared/zod + shared/router | → 类型共享扩展（RowOf 面） |

框架面已验证：`RowOf<SHAPES.agents>` / `BodyOf<S>` 类型导出（orm.ts——类型面
单源）；前端 `lib/types.ts` 的 Agent 与后端 `SHAPES.agents` 字段**同构**
（漏字段 = 手动双写必然漂移——shapes-alignment 测试只保后端侧）。

## 波次

| 波次 | 内容 | 验收 |
| --- | --- | --- |
| W0 | **类型单源跨端**（机制 + 试点）：**前置探针**（30 分钟——跨目录 `import type` 的路径面/循环依赖面（ui/ 与 src/ 边界）· 派生类型在 IDE 的 hover 可读性（`RowOf<SHAPES.agents>` 展开面））；框架把后端 shape 类型面稳定导出（`RowOf`/`BodyOf` 已有）；平台试点——`ui/lib/types` 的 **Agent/Department 等 2 个** 改从 `SHAPES` 派生（+ 响应附加字段交叉）——37 interface 开始下降 | 探针报告（路径/循环/可读性 三面裁决）+ 试点 diff（派生替换——无字段漂移——tsd 断言）+ 平台 tsc 0 |
| W1 | **端点类型面评估**：探针——apiClient 端点注册表（URL → 方法/入参/出参类型——74 处可收口？）可行性 1 天探针——**判负或立项**（端点是动态拼接（query 参数）——注册表表达力评估） | 探针报告（可行性 + 样例 3 处）+ 判负/立项裁决 |
| W2 | **权限单源**：roles 声明共享（`canWrite`/`requireWriter` 从同一份 roles 定义派生——后端 21 处 + 前端 8 处消费同源） | 单源文件（roles.ts 共享）+ 前后端 tsc 0 + 行为等价测试 |
| W3 | **全栈测试动线**：契约（memory 单测）→ 场景（playwright SSR）→ 平台 e2e（真 server + memory）三层动线文档 + **契约测试模板**（第一个测试 5 行示范——memory orm + handler 直调 + 状态码断言——新增开发者照抄即可）+ 每层哨兵（新增 route 的测试覆盖哨兵——audit 挂） | docs 增补 + 模板文件 + 哨兵上线（新 route 无测试 = 黄/红） |
| W4 | **docs + 回归门**：docs/server.md §5 全栈类型共享增补 + 认证接线时序契约 + 全量回归门 | 五域+audit 七线 · tsc 三 0 · 平台 475 |

## 判负记录（可被新论证推翻）

- **37 interface 全量派生**：不做——W0 试点 2 个验证机制 + 判据（纯 API
  响应类型可派生；**含前端专属字段**（格式化/UI 态）手动扩展）——按需
  演进；推翻：漂移 bug 实例出现（测试抓出字段不同步）
- **端点注册表**：待 W1 探针裁决——预判判负（URL 动态拼接 + query 参数
  表达力不足——74 处多是一次性调用）；推翻：探针显示注册表可覆盖 ≥80%
  调用且不引入 any
- **SSR 全页面化**：不做（ROADMAP 边界——auth token localStorage——登录/
  注册 only）；推翻：SEO 需求出现（内容页级）
- **跨端类型生成管线**（codegen——后端 shape → 前端 .d.ts 文件）：不做——
  **类型共享（import）优于类型生成**（生成面有同步/漂移问题——文件产物
  天然落后于源）；推翻：跨目录 import 面探针显示不可行（循环依赖/路径
  不可解）

## 执行实录（边做边记）

（待 W0 起填）

## 验收标准

- [ ] W0：类型派生试点（2 个界面类型——37 开始下降）· tsd 断言 · tsc 0
- [ ] W1：端点探针报告 + 判负/立项裁决（实录登记）
- [ ] W2：权限单源（前后端消费同源）· 行为等价测试
- [ ] W3：三层测试动线 docs + 覆盖哨兵上线
- [ ] W4：docs §5 增补 · 全量回归门（五域 + audit 七线 · tsc 三 0 · 平台 475）
