# RENDER-HEALTH-PLAN — vdom 渲染健康全面优化计划

> 发起动因：**「这个问题已经出现多次」**——近三周 vdom 相关问题的共性：
> ① 交付物渲染循环（FilesSection 每拍 remount——应用层断链——**框架根因未修**）
> ② /admin 卡住（1292 租户全量渲染 2.4s——截断——**规模防线缺失**）
> ③ 流式慢（渲染循环占主线程 85%——① 的症状）
> ④ hover 闪烁（① 的症状）
> ⑤ 打字卡顿（历史——每键全页渲染——已修——同源：渲染粒度）
>
> **共同模式：渲染的「健康」缺少度量与防线——频率/规模/复用失败率**
> 全部靠用户报 + 每次重查。本计划 = 诊断器基建（预防）+ 根因修复（治本）+
> 调度/规模防线（治标兜底）+ 测试锁定（不复燃）。

---

## 1. 渲染健康三轴（问题分类框架）

| 轴 | 度量 | 症状 | 历史案例 |
|---|---|---|---|
| **频率** | 渲染次数/秒（稳态 > 5/s = 可疑） | 卡顿/闪烁/流式慢 | ①交付物循环（15/s）⑤打字（每键） |
| **规模** | 单次渲染节点数/命令数（> 5000 = 可疑） | 首屏慢/冻结 | ②admin（1292 租户） |
| **复用** | 组件工厂重跑率（> 5% = 可疑） | 状态丢失/重建风暴 | ①（83 次/6s）——**框架根因未解** |

**诊断器 = 三轴仪表**（dev 模式——页面内 `__wfRenderHealth`）——**问题出现即读数**——不再盲查。

---

## 2. 波次计划（每波独立验收可回退）

### 波次 1：诊断器基建（RENDER-HEALTH 探针——dev 模式）✅ 2027-09

**落地**：`src/client/vdom/dev/render-health.ts`——三轴读数经**已流化的
诊断基础设施**（applied$/complete$——VDOM-OBSERVABLE-COMPLETE 波次 1）上
取数——频率=complete$ 计数窗口、规模=applied$ 命令数、复用=seg:create/
seg:reuse spy 事件（引擎零侵入——spyEvent 门控）——`window.__wfRenderHealth`
每 2s 滚动 + 阈值 warn（频率 >10/s · 规模 >5000 · 单渲染 >300ms · 重跑率
>5%）——serve `__WF_DEV__` 门控——生产零成本——契约测试 6（render-health）

**目标**：渲染健康可视化——问题「发生即读数」。

**任务**：
1. `src/client/vdom/dev/render-health.ts`——三轴计数（serve 级挂载）：
   - 频率：render 次数/时间窗（runRender 入口）
   - 规模：命令数/渲染（renderCtx.stream 的流计数——transform 包装）
   - 复用：renderComponent 的 isNew/mount 计数 + 工厂名（dev 收集）
2. 仪表输出：`window.__wfRenderHealth`（每 2s 滚动——dev-only 零生产成本）
3. `console.warn` 阈值（频率 > 10/s / 单渲染 > 300ms——**超阈值即警**——同
   effect-guard 模式——开发期红线）
4. dev 页面按钮（可选——`?wfhealth=1` 打开悬浮仪表）

**验收**：契约测试（仪表计数正确性——freq/scale/reuse 三数）· dev 模式产生
`__wfRenderHealth` · 生产零装（门控）——tsc 0——存量全绿

### 波次 2：根因修复——diff 组件复用失败（核心）

**目标**：治愈「每拍 remount」——**先最小复现（契约测试）再修**——不猜。

**任务**：
1. **最小复现**（契约层——node 直跑——零浏览器）：
   - 组件树：`div > [cond ? A : null, keyed Comp]`——交替渲染——断言 Comp
     工厂执行次数（应 1——复用）vs 实际（>1 = 复现）
   - 候选场景（基于上次证据）：keyed 组件在**混合列表**（keyed+unkeyed 混排）
     / 组件输出对照（diffComponentOutput 的 keyed 路径）/ div children 的
     **投影**（FRAG 展开）
2. **定位**（复现后——二分 diff 路径）：
   - diffChildrenItems 的分类分支（all-keyed/unkeyed/mixed——**每个分支的
     keyed 项处理**）
   - emit（createRenderDispatcher）组件分支 vs diffSame 的组件对照——
     **「走 emit（全量渲染）而非 diffSame（对照复用）」的分支 = 嫌疑**
   - renderComponent 的 rec 查找（keyedId 一致性——build/diff 双实现漂移）
3. **修复**：嫌疑分支统一到「对照复用」——**单一实现源**（diffSame——同
   OBSERVABLE-ARCH 的「输出对照单一实现源」纪律）
4. **回归防护**：契约测试锁定（复用次数断言——工厂不重跑）——场景层
   component-reuse 扩展

**验收**：最小复现绿（复用 1 次）· 契约/场景全绿 · 交付物循环的「框架层」
  复测（去掉应用层静默——**框架层已修复**——验证）
   ——应用层静默降级为「性能优化」（非「断链补丁」——**层级正确**）

### 波次 3：渲染调度 batching（render$ 流——频率防线）

**目标**：同拍 N 次 render → 1 次渲染循环（React 18 同级优化——频率轴兜底）。

**任务**：
1. `render$` 请求流（serve 内——render() = 流 next——替代立即渲染循环）
2. buffer + 微任务 flush：同拍 N 次 → 1 次（renderPhase 重入合并）
3. 「渲染中入队」语义由流合并承接（替换手写 FIFO——**代码简化**）
4. SSR 端直通（单次渲染——无调度）

**契约测试**：batching（同拍 N → 1）· 入队合并 · 顺序语义保持

**验收**：契约绿 · 打字/流式场景性能复测（频率轴读数下降）· 场景全绿

### 波次 4：规模防线（虚拟列表基座——规模轴兜底）

**目标**：长列表渲染不冻结（admin/日志/AI 会话——规模场景的通用解法）。

**任务**：
1. `VirtualList` 基座（framework 组件——窗口渲染——已有 VirtualTable——
   提取通用基座 + 尺寸测量/滚动窗口）
2. admin 表格接入（1292 租户——**虚拟化替代截断**——完整列表可浏览）
3. 长文本渲染粒度（AI 会话消息——窗口渲染可选）

**验收**：admin 全量 1292 渲染 < 500ms · 场景全绿 · VirtualList 组件测试

### 波次 5：防线测试 + 文档（不复燃）

**目标**：预防——把「再次发生」变成「测试失败」。

**任务**：
1. **渲染健康契约测试**（三轴基准——频率/规模/复用的阈值断言——同
   layout-inventory 的计数基线制）
2. **循环检测回归**（特定场景：稳态 3s 渲染计数 < N——交付物/chat 锁定）
3. 文档：AGENTS.md 补「渲染健康」章节（三轴 + 诊断器用法 + 阈值红线）
4. RENDER-HEALTH-PLAN 完成标记

**验收**：全部防线测试绿 · 文档 · 全量回归（246+116+200+289 + tsc 双侧 0）

---

## 3. 里程碑估计

| 波次 | 预估 | 关键风险 | 回退点 |
|---|---|---|---|
| 1 | 2-3 天 | 探针精度（误报/漏报） | 纯新增——门控——可禁用 |
| 2 | 3-5 天 | 根因可能多分支（mixed/投影/输出对照） | 最小复现先行——每分支独立修复 |
| 3 | 2-3 天 | batching 与现有渲染队列语义 | 契约锁定——可回退 |
| 4 | 3-4 天 | 虚拟化与现有 Table 集成 | 提取基座——不破坏现有 |
| 5 | 1-2 天 | — | — |

**总预估：12-17 天**——**波次 2 是核心**（根因）——1/3/4 是防线（防复发）——
**顺序不可换**（诊断器先行——后续修复有度量；根因修复后——防线闭环）。

---

## 4. 验收标准（总）

1. **根因治愈**：最小复现 + 真实页面（交付物循环——框架层——应用层静默
   降级为优化）——**复用率 > 95%**（诊断器读数）
2. **防线就位**：三轴仪表 dev 可用——阈值告警——契约测试锁定
3. **性能基线**：chat 稳定 < 5 渲染/s · admin 1292 全量 < 500ms ·
   流式 token 渲染无长任务（> 30ms = 0）
4. **零回归**：246 + 116 + 200 + 289 + tsc 双侧 0
5. **文档**：AGENTS.md 渲染健康章节——作者可自查

---

## 5. 为什么「多次出现」——纪律层面的根因

| 层面 | 缺失 | 补法 |
|---|---|---|
| 度量 | 无渲染健康读数——问题靠用户报 | 波次 1（诊断器） |
| 根因 | diff 复用失败未根治（每次都应用层绕） | 波次 2（最小复现 + 治本） |
| 防线 | 无频率/规模阈值 | 波次 3/4 + 波次 5 测试 |
| 层级 | 应用层补丁掩盖框架根因（上次） | 波次 2 的「层级验证」步骤（去补丁复测） |
